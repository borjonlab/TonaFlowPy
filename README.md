# TonaFlowPy
The python version of tonaflow.

# Install
```
cd TonaFlowPy #or wherever you want TonaFlow to be
git clone https://github.com/borjonlab/TonaFlowPy.git

python -m venv ./venv
source ./venv/bin/activate 
pip install -r requirements.txt
```
